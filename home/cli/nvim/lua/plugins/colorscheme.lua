return {
  --  "LazyVim/LazyVim",
  --  opts = {
  --    colorscheme = "default",
  --  },
  --"rcarriga/",
  --config = function(plugin, opts)
  --  require("plugins.configs.notify")(plugin, opts)
  --  local notify = require("notify")
  --  notify.setup({
  --    background_colour = "#eeeff4",
  --  })
  --end,
}
